def sign(f: float) -> float:
	"""Returns the sign of f.
	Return value is 1 when f is positive or zero, -1 when f is negative.

	
	Args:
	    f (float): The value to determine the sign of
	
	Returns:
	    float: The sign of f
	"""
	return 1 if f >= 0 else -1